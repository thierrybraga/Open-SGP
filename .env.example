# ==================================================================
# ARQUIVO DE EXEMPLO - COPIE PARA .env E CONFIGURE OS VALORES
# ==================================================================
# Este arquivo contém as variáveis de ambiente necessárias para
# executar o ISP ERP. NUNCA commite o arquivo .env com valores reais!
# ==================================================================

# ==================================================================
# AMBIENTE
# ==================================================================
# Valores possíveis: development, production, testing
ENVIRONMENT=development

# ==================================================================
# SEGURANÇA - OBRIGATÓRIO MUDAR EM PRODUÇÃO
# ==================================================================
# ATENÇÃO: Gere uma chave segura para produção!
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_IN_PRODUCTION_USE_STRONG_SECRET_KEY

# Chave de criptografia para credenciais (Fernet)
# Gere com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# CRÍTICO: Essa chave criptografa senhas SMTP, API keys, etc.
# Se perder essa chave, todas as credenciais ficam inacessíveis!
ENCRYPTION_KEY=

# Algoritmo de criptografia JWT
JWT_ALGORITHM=HS256

# Tempo de expiração do token JWT em minutos (padrão: 1440 = 24 horas)
JWT_EXPIRATION_MINUTES=1440

# ==================================================================
# BANCO DE DADOS
# ==================================================================
# PostgreSQL connection string
# Formato: postgresql://usuario:senha@host:porta/nome_banco
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/isp_erp

# Pool de conexões
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=0

# ==================================================================
# REDIS
# ==================================================================
# Redis connection string para cache e filas
REDIS_URL=redis://localhost:6379/0

# ==================================================================
# CORS
# ==================================================================
# Lista de origens permitidas (separadas por vírgula)
# Em desenvolvimento: http://localhost:3000,http://localhost:8080
# Em produção: https://seudominio.com
CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:8080

# ==================================================================
# SMTP (Email)
# ==================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu_email@gmail.com
SMTP_PASSWORD=sua_senha_ou_app_password
SMTP_FROM_EMAIL=noreply@suaempresa.com
SMTP_FROM_NAME=ISP ERP

# ==================================================================
# SMS
# ==================================================================
# Provedor de SMS (twilio, zenvia, etc)
SMS_PROVIDER=twilio
SMS_API_KEY=sua_api_key
SMS_API_SECRET=seu_api_secret
SMS_FROM_NUMBER=+5511999999999

# ==================================================================
# WHATSAPP
# ==================================================================
# API do WhatsApp Business (ex: Evolution API, WPPConnect, etc)
WHATSAPP_API_URL=http://localhost:8000
WHATSAPP_API_KEY=sua_api_key
WHATSAPP_SESSION_NAME=isp_erp

# ==================================================================
# FISCAL (Notas Fiscais)
# ==================================================================
# Certificado A1 para assinatura de NFe/NFSe (base64)
FISCAL_CERTIFICATE_A1_BASE64=
FISCAL_CERTIFICATE_A1_PASSWORD=

# Ambiente SEFAZ (1=Produção, 2=Homologação)
FISCAL_SEFAZ_ENVIRONMENT=2

# IBGE código do município
FISCAL_MUNICIPALITY_CODE=3550308

# ==================================================================
# MIKROTIK (Gerenciamento de Rede)
# ==================================================================
# Credenciais padrão para dispositivos MikroTik
MIKROTIK_DEFAULT_USERNAME=admin
MIKROTIK_DEFAULT_PASSWORD=

# ==================================================================
# RADIUS
# ==================================================================
# Configuração do servidor RADIUS (FreeRADIUS)
RADIUS_SERVER_HOST=localhost
RADIUS_SERVER_PORT=1812
RADIUS_SECRET=testing123

# ==================================================================
# GATEWAY DE PAGAMENTO
# ==================================================================
# Exemplo: Stripe, PagSeguro, Asaas, etc
PAYMENT_GATEWAY_PROVIDER=asaas
PAYMENT_GATEWAY_API_KEY=
PAYMENT_GATEWAY_ENVIRONMENT=sandbox

# ==================================================================
# ARMAZENAMENTO (Storage)
# ==================================================================
# Provedor de storage (local, s3, minio)
STORAGE_PROVIDER=local

# Local storage path
STORAGE_LOCAL_PATH=./storage

# S3 / MinIO
STORAGE_S3_ENDPOINT=
STORAGE_S3_ACCESS_KEY=
STORAGE_S3_SECRET_KEY=
STORAGE_S3_BUCKET=isp-erp
STORAGE_S3_REGION=us-east-1

# ==================================================================
# LOGGING
# ==================================================================
# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ==================================================================
# SENTRY (Monitoramento de Erros)
# ==================================================================
# DSN do Sentry para rastreamento de erros
SENTRY_DSN=

# ==================================================================
# BACKUP
# ==================================================================
# Diretório para backups automáticos
BACKUP_PATH=./backups

# Retenção de backups em dias
BACKUP_RETENTION_DAYS=30

# ==================================================================
# API EXTERNA - CEPS
# ==================================================================
# API para busca de CEP (ViaCEP, etc)
CEP_API_URL=https://viacep.com.br/ws

# ==================================================================
# RATE LIMITING
# ==================================================================
# Limites globais de requisições
RATE_LIMIT_DEFAULT=100/minute
RATE_LIMIT_AUTH=5/minute
RATE_LIMIT_API=1000/hour

# ==================================================================
# FEATURE FLAGS
# ==================================================================
# Habilita/desabilita funcionalidades
FEATURE_FISCAL_ENABLED=true
FEATURE_SMS_ENABLED=true
FEATURE_WHATSAPP_ENABLED=true
FEATURE_PAYMENT_GATEWAY_ENABLED=true
FEATURE_BACKUP_ENABLED=true

# ==================================================================
# NOTAS IMPORTANTES
# ==================================================================
# 1. NUNCA commite o arquivo .env no git
# 2. Adicione .env ao .gitignore
# 3. Em produção:
#    - Use SECRET_KEY forte e único
#    - Configure CORS para domínio específico
#    - Use HTTPS para todas as URLs
#    - Habilite apenas features necessárias
#    - Configure backups automáticos
#    - Configure Sentry para monitoramento
# ==================================================================
